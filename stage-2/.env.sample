# Required Azure AI Foundry configuration
AZURE_AI_PROJECT_ENDPOINT=https://<ai-foundry-resource>.services.ai.azure.com/api/projects/<project-name>
AZURE_AI_ASSISTANT_ID=asst_<your-assistant-id>

# Optional runtime tuning (defaults: 1.0 seconds / 120 seconds)
RUN_POLL_DELAY_SECONDS=1.0
RUN_MAX_WAIT_SECONDS=120.0

# Authentication (choose the mechanism you use with DefaultAzureCredential)
# Uncomment and populate for a service principal.
# AZURE_CLIENT_ID=<client-id>
# AZURE_TENANT_ID=<tenant-id>
# AZURE_CLIENT_SECRET=<client-secret>
